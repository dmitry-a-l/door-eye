[Unit]
Description=Monitor door & lock statuses to autoclose door
After=network.target

[Service]
ExecStart=/usr/local/bin/lockctl auto_close_lock
Restart=always

[Install]
WantedBy=multi-user.target

